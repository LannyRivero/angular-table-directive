import { TableSortFilter } from './table-sort-filter';

describe('TableSortFilter', () => {
  it('should create an instance', () => {
    const directive = new TableSortFilter();
    expect(directive).toBeTruthy();
  });
});
